{
  "name": "angular-basic",
  "private": true,
  "version": "0.0.0",
  "description": "A starter project to learn AngularJS",
  "devDependencies": {
    "http-server": "^0.6.1"
  },
  "scripts": {
    "prestart": "npm install",
    "start": "http-server -p 8000"
  }
}
