{
  "name": "angular-basic",
  "private": true,
  "version": "0.0.0",
  "description": "A starter project to learn AngularJS",
  "devDependencies": {
    "http-server": "^0.6.1"
  },
  "scripts": {
    "postinstall": "bower install --allow-root",
    "prestart": "npm install",
    "start": "http-server -a 0.0.0.0 -p 8000"
  }
}
